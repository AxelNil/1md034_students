const vm = new Vue({
  el: 'Burgers',
  data: {
    arbitraryVariableName: 'Välj en burgare2'
  }
})

const vm = new Vue({
  el: '#myID',
  data: {
    /* Then you can copy it to the Vue object like this */
    b1 : WTM.itemName,
    b2 : FTL.itemName,
    b3 : TJR.itemName,
    b4 : TFB.itemName,
    b5 : TEB.itemName,
  }
})

const vm = new Vue({
el: '#myID',
data: {
  menu
  }
})
